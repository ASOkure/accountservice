server:
  port: 8081
spring:
  config:
    activate:
      on-profile: "prod"
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
build:
  version: "1.0"

accounts:
  message: "Welcome to Bank accounts related prod APIs "
  contactDetails:
    name: "Ethan Okure - product Owner"
    email: "ethan@mantletechsolutions.com"
  onCallSupport:
    - (+44) 7741368-777
    - (+44) 7741368-777
