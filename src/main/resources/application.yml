server:
  port: 8081
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
build:
  version: "1.0"

accounts:
  message: "Welcome to Bank accounts related local APIs "
  contactDetails:
    name: "Sam Okure - Developer"
    email: "john@mantletechsolutions.com"
  onCallSupport:
    - (+44) 7741368-555
    - (+44) 7741368-555
